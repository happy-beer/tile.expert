# PostgreSQL init scripts

Place PostgreSQL-compatible SQL files here to initialize a fresh database.

Important: the provided `dump.sql` is in MySQL dialect and must be converted
before using it in this directory.
